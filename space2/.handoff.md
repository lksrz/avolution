# Handoff Notes

## Status
- Loop 4 COMPLETE
- Major improvement: all signals now continuous (no dead zones / arbitrary thresholds)
- Updated target=21:50 from UP to DOWN based on improved model
- Results: 1 verified (target=21:40 wrong), 2 PENDING (target=21:45, target=21:50)
- Score so far: 0/1 — need to improve

## Architecture
- `predictor.py` - Run `python3 predictor.py`
  - `verify_all_pending()` scans log for ALL PENDING entries
  - Makes prediction using 12-signal continuous model (no dead zones)
  - Prints debug output showing all signal contributions
  - Updates predictions within same window if signal changes
- `state.json` - Current window state
- `predictions.log` - Append-only (PENDING entries updated in-place)
- All in `/home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/`

## Signal Model (v17 - continuous)
All signals produce continuous values (no thresholds → no dead zones):
1. **mom** - 3-candle direction momentum
2. **t5** - 5-candle price trend (proportional)
3. **t10** - 10-candle price trend (proportional)
4. **sma** - Price vs 10-SMA deviation (mean revert)
5. **rsi** - RSI centered at 50 (continuous)
6. **ema** - EMA 5/12 crossover (continuous)
7. **bb** - Bollinger position centered at 0.5 (mean revert)
8. **cur** - Current incomplete candle momentum
9. **alt** - Alternation pattern
10. **vol** - Volume + body continuation
11. **mr_dn/mr_up** - Mean reversion on 4+ streaks

## Each Loop Priority:
1. Run `python3 predictor.py` (verify + predict + log)
2. Git commit changes
3. If >120s remain: improve strategy
4. Update this handoff file

## Key Insight from Loop 4
- Previous model had too many thresholds creating dead zones
- Most signals were barely below thresholds → produced 0 → model defaulted to weak momentum
- Continuous signals allow many weak bearish signals to collectively outweigh one weak bullish signal
- This is why the model kept predicting UP despite a clear downtrend
