# Handoff Notes

## Status
- Loop 1 COMPLETE: Initial setup done
- Git repo initialized with first commit
- predictor.py built with RSI, EMA, SMA, momentum, volume, streak signals
- 2 predictions logged so far (target=21:40 UP, target=21:45 UP)

## Architecture
- `predictor.py` - Main engine. Run `python3 /home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/predictor.py`
  - Fetches 20 candles from Binance
  - Verifies previous prediction if target candle closed
  - Updates predictions.log with actual results
  - Makes new prediction using multi-signal model
  - Saves state.json
- `state.json` - Current window state, tracks what we predicted
- `predictions.log` - Append-only log, tamper-proof in git
- All files in `/home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/`

## Each Loop Priority:
1. Read `.loop_meta.json` and `.time_remaining.json`
2. Run `python3 predictor.py` (handles verify + predict + log)
3. Git commit predictions.log changes
4. If >120s remain: improve predictor strategy
5. Update this handoff file

## Improvement Ideas
- Add Bollinger Bands signal
- Track prediction accuracy and adapt weights
- Add support for reading incomplete current candle for intra-window updates
- Consider fetching 1m candles for finer-grained analysis near deadline
