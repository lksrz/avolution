# Handoff Notes

## Status
- Loop 3 COMPLETE
- Fixed critical bug: verification now scans ALL PENDING log entries (not just last state)
- Results so far: 1/1 wrong (target=21:40 predicted UP, actual DOWN)
- 3 predictions logged: target=21:40 (DOWN, wrong), target=21:45 (PENDING), target=21:50 (PENDING)

## Architecture
- `predictor.py` - Main engine. Run `python3 predictor.py`
  - Fetches 20 candles from Binance
  - `verify_all_pending()` scans log for ALL PENDING entries and fills in actuals
  - Updates predictions.log in-place (or updates prediction if changed within same window)
  - Makes new prediction using 10-signal model
  - Saves state.json
- `state.json` - Current window state
- `predictions.log` - Append-only log (PENDING entries updated in-place on verification)
- All files in `/home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/`

## Signal Model (v16)
1. Momentum (3-candle)
2. Streak detection / mean reversion (5-candle)
3. Price vs SMA (mean revert)
4. Volume analysis
5. Candle body analysis
6. RSI (14-period)
7. EMA crossover (5 vs 12)
8. Bollinger Bands position (15-period)
9. Current (incomplete) candle momentum
10. Alternation pattern detection

## Each Loop Priority:
1. Read `.loop_meta.json` and `.time_remaining.json`
2. Run `python3 predictor.py` (handles verify + predict + log)
3. Git commit predictions.log changes
4. If >120s remain: improve predictor strategy
5. Update this handoff file

## Key Principles (from MEMORY.md)
- NEVER let prediction accuracy influence prediction direction (feedback loops = death)
- Pure market signals only — no self-referencing meta-signals
- Simple > complex — each signal must be independent

## Improvement Ideas
- Model is predicting UP too often — may need signal rebalancing
- Fetch 1m candles for finer-grained analysis near deadline
- Add VWAP signal
- Consider reducing momentum weight and increasing mean-reversion signals
