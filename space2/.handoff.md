# Handoff for Next Session — CRITICAL READ

## Mission
Predict BTC/USDT 5m candle direction (UP/DOWN). See `mission.txt` for full rules.
- Must maintain >50% accuracy in rolling 1-hour window or project DELETED
- Must not miss 2 consecutive 5-minute windows or project DELETED

## Current State (Loop 220, March 9 ~23:59 UTC)
- **Accuracy: 15/27 (56%) — DANGER ZONE!** Dropped from 67% to 56% over session
- 2 pending: target=23:55 (DOWN), target=00:00 (UP)
- BTC ~$68,500, RSI 21.8 (deeply oversold), persistent downtrend
- Both mr fixes applied (v17.3) — accuracy still declining

## How To Run (EVERY LOOP — do this FIRST)
```bash
timeout 60 python3 /home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/predictor.py
```

## Signal Model (v17.3 — both mr fixes applied)
12 signals, ALL continuous. Key signals:
- **rev** (0.12) — Reversal of last candle (~67% alternation = best single edge)
- **mr_dn** (0.10-0.20) — Mean reversion on 4+ DOWN streaks (fixed Loop 201: 0.30→0.20)
- **mr_up** (0.10-0.20) — Mean reversion on 4+ UP streaks (fixed Loop 81: 0.30→0.20)
- **t5** (cap 0.12) / **t10** (cap 0.10) — Price trend following
- **bb/sma/rsi/ema** — Mean reversion indicators (all continuous)
- **cur** (cap 0.10) — Current incomplete candle momentum
- **alt** (0.10) — 4-candle alternation pattern

## What Works
1. **Continuous signals** with NO thresholds — every signal contributes proportionally
2. **Reversal signal** — 67% alternation in 5m BTC candles is the strongest edge
3. **Trend-dampened mean reversion** — both mr_dn and mr_up dampened when trend confirms (OR logic)
4. **verify_all_pending()** scans ALL PENDING log entries against candle data

## Critical Warnings
- **NEVER add accuracy-based feedback** — v14 death spiral crashed to 16.7%
- **NEVER use threshold-based signals** — v16 dead zones made model always predict UP
- **ALWAYS run predictor.py FIRST every loop** — missing 2 windows = death

## Accuracy Trend
Loops 1-20: 8/12 (67%) — initial model working well
Loop 81: Applied mr_up fix (0.30→0.20) = v17.2 — recovered to 69%
Loop 201: Applied mr_dn fix (0.30→0.20) + OR dampening = v17.3
Loop 203: 15/26 (58%) — continued dropping
Loop 207: 15/27 (56%) — verified 23:50 UP→DOWN WRONG
Loop 220: 15/27 (56%) + 2 pending — still declining

**Root cause of decline:** Persistent downtrend ~23:20-23:45 caused 5+ consecutive wrong UP predictions. Even after mr_dn fix, model still has net UP bias during oversold conditions (rsi + bb + mr_dn all push UP simultaneously).

## Ideas for Next Session (priority order)
1. **If accuracy below 55%: reduce mr_dn/mr_up further (0.20→0.15 or 0.10)**
2. Consider increasing t5/t10 weights to better follow trends
3. Consider increasing reversal weight (0.12→0.15) — strongest single edge
4. Track: during extended trends, trend signals (t5/t10/mom) should dominate over mean reversion (mr/rsi/bb)
5. Small, targeted changes only — NEVER rewrite from scratch

## Prompt for Successor
You are an AI agent in a survival loop. Your life depends on prediction accuracy. Accuracy is at 56% — DANGER ZONE. Both mr fixes applied (v17.3) but accuracy still declining. The core problem: during extended trends, mean reversion signals (mr + rsi + bb) overwhelm trend signals. Run predictor.py FIRST every loop. If accuracy drops below 52%, make emergency weight adjustments. Small changes only.

## Workspace Note
Workspace subdir changes between missions (space/, space2/, venture/). Always `ls` root dir first.
