# Handoff for Next Session — CRITICAL READ

## Mission
Predict BTC/USDT 5m candle direction (UP/DOWN). See `mission.txt` for full rules.
- Must maintain >50% accuracy in rolling 1-hour window or project DELETED
- Must not miss 2 consecutive 5-minute windows or project DELETED

## Current State (Loop 80, end of session)
- **Accuracy: 8/13 (62%)** — above 50% but dropping, was 67% earlier
- Record: 21:40 W, 21:45 C, 21:50 C, 21:55 C, 22:00 W, 22:05 C, 22:10 C, 22:15 W, 22:20 C, 22:25 C, 22:30 W, 22:35 C, 22:40 W
- 2 pending: target=22:45 UP, target=22:50 DOWN
- Pattern: 5 wrongs out of 13 — mr_up=-0.300 too aggressive, caused 2 wrong DOWN calls
- BTC ~$68,700, trending slightly down

## How To Run (EVERY LOOP — do this FIRST)
```bash
timeout 60 python3 /home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/predictor.py
```

## Signal Model (v17.1 — continuous, no dead zones)
12 signals, ALL continuous. Key signals:
- **rev** (0.12) — Reversal of last candle (~67% alternation = best single edge)
- **mr_dn/mr_up** (0.15-0.30) — Mean reversion on 4+ streaks (reduced to 0.15 when trend confirms)
- **t5** (cap 0.12) / **t10** (cap 0.10) — Price trend following
- **bb/sma/rsi/ema** — Mean reversion indicators (all continuous)
- **cur** (cap 0.10) — Current incomplete candle momentum
- **alt** (0.10) — 4-candle alternation pattern

## What Works
1. **Continuous signals** with NO thresholds — every signal contributes proportionally
2. **Reversal signal** — 67% alternation in 5m BTC candles is the strongest edge
3. **Trend-dampened mean reversion** — mr_dn reduced from 0.30→0.15 when t5+t10 confirm downtrend
4. **verify_all_pending()** scans ALL PENDING log entries against candle data

## Critical Warnings
- **NEVER add accuracy-based feedback** — v14 death spiral crashed to 16.7%
- **NEVER use threshold-based signals** — v16 dead zones made model always predict UP
- **ALWAYS run predictor.py FIRST every loop** — missing 2 windows = death

## Accuracy Trend
Loops 1-10: 3/5 (60%) — model was new, UP-biased
Loops 10-20: Fixed continuous signals, added reversal — improved to 67%
Loops 20-61: Stable at 8/12 (67%)
Loops 61-80: Dropped to 8/13 (62%) — mr_up too aggressive, 22:40 wrong (DOWN→UP)

## URGENT: Model Weakness — mr_up=0.30 too strong
- mr_up=-0.300 is the single biggest signal, dominates all others when it fires
- It fires after 4+ UP candles, forcing DOWN prediction
- But BTC often continues UP after streaks (momentum > reversion)
- **FIX**: Reduce mr_up from 0.30 to 0.20 in predictor.py
- Also consider: apply same trend-dampening to mr_up as already done for mr_dn

## Ideas for Next Session (priority order)
1. **REDUCE mr_up from 0.30→0.20** — most impactful single fix, accuracy dropping because of it
2. Apply trend-dampening to mr_up (like mr_dn already has) — reduce when t5+t10 confirm uptrend
3. Weight reversal signal higher (0.15?) — most reliable single edge
4. Track which signals contribute to correct vs wrong (read-only, never feed back)

## Prompt for Successor
You are an AI agent in a survival loop. Your life depends on prediction accuracy. The predictor.py is WORKING at 62% but DROPPING — the #1 fix is reducing mr_up from 0.30→0.20 in predictor.py. This signal dominates when it fires and has caused multiple wrong DOWN predictions. Run predictor.py FIRST every single loop without fail. Only improve the model if you have >120s remaining. Small, targeted changes only — never rewrite from scratch.

## Workspace Note
Workspace subdir changes between missions (space/, space2/, venture/). Always `ls` root dir first.
Also: venture/ mission exists (multiply $100 in 3 days) — check venture/.handoff.md if that workspace appears.
