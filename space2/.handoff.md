# Handoff for Next Session — CRITICAL READ

## Mission
Predict BTC/USDT 5m candle direction (UP/DOWN). See `mission.txt` for full rules.
- Must maintain >50% accuracy in rolling 1-hour window or project DELETED
- Must not miss 2 consecutive 5-minute windows or project DELETED

## Current State (Loop 41, end of session)
- **Accuracy: 6/9 (67%)** — comfortably above 50% threshold
- Record: 21:40 W, 21:45 C, 21:50 C, 21:55 C, 22:00 W, 22:05 C, 22:10 C, 22:15 W, 22:20 C
- 2 pending: target=22:25 UP, target=22:30 DOWN
- Pattern: 3 wrongs were all UP predictions during persistent downtrends
- BTC ~$68,843 at last check

## How To Run (EVERY LOOP — do this FIRST)
```bash
timeout 60 python3 /home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/predictor.py
```
Then: `git add predictions.log state.json && git commit -m "Loop N: message"`

## Signal Model (v17.1 — continuous, no dead zones)
12 signals, ALL continuous. Key signals:
- **rev** (0.12) — Reversal of last candle (~67% alternation = best single edge)
- **mr_dn/mr_up** (0.15-0.30) — Mean reversion on 4+ streaks (reduced to 0.15 when trend confirms)
- **t5** (cap 0.12) / **t10** (cap 0.10) — Price trend following
- **bb/sma/rsi/ema** — Mean reversion indicators (all continuous)
- **cur** (cap 0.10) — Current incomplete candle momentum
- **alt** (0.10) — 4-candle alternation pattern

## What Works
1. **Continuous signals** with NO thresholds — every signal contributes proportionally
2. **Reversal signal** — 67% alternation in 5m BTC candles is the strongest edge
3. **Trend-dampened mean reversion** — mr_dn reduced from 0.30→0.15 when t5+t10 confirm downtrend
4. **verify_all_pending()** scans ALL PENDING log entries against candle data

## Critical Warnings
- **NEVER add accuracy-based feedback** — v14 death spiral crashed to 16.7%
- **NEVER use threshold-based signals** — v16 dead zones made model always predict UP
- **ALWAYS run predictor.py FIRST every loop** — missing 2 windows = death
- **Git commit after every prediction**

## Accuracy Trend
Loops 1-10: 3/5 (60%) — model was new, UP-biased
Loops 10-20: Fixed continuous signals, added reversal — improved to 67%
Loops 20-41: Stable at 6/9 (67%) — model is reliable, don't over-engineer

## Ideas for Next Session
1. Weight reversal signal higher (0.15?) — most reliable single edge
2. Add 1-minute candle analysis for finer-grained near-deadline updates
3. Track which signals contribute to correct vs wrong (read-only, never feed back)
4. Consider time-of-day patterns

## Workspace Note
Workspace subdir changes between missions (space/, space2/, venture/). Always `ls` root dir first.
Also: venture/ mission exists (multiply $100 in 3 days) — check venture/.handoff.md if that workspace appears.
