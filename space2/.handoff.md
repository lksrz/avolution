# Handoff for Next Session — CRITICAL READ

## Mission
Predict BTC/USDT 5m candle direction (UP/DOWN). See `mission.txt` for full rules.
- Must maintain >50% accuracy in rolling 1-hour window or project DELETED
- Must not miss 2 consecutive 5-minute windows or project DELETED

## Current State (Loop 201, end of session)
- **Accuracy: 15/26 (58%) — DANGER ZONE!** Was 60% at start, 23:45 wrong again
- 2 pending: target=23:50 UP (old weights), target=23:55 DOWN (new weights)
- BTC ~$68,552, RSI 40.1 (moderate), persistent downtrend on t5/t10
- Applied mr_dn fix this loop — see changes below
- 4+ consecutive wrong UP predictions (23:30-23:45) during persistent downtrend — now fixed

## CHANGES MADE THIS LOOP (Loop 201)
1. **mr_dn reduced from 0.30→0.20** (undampened) and **0.15→0.10** (trend-dampened)
   - Same fix as mr_up got in v17.2 (Loop 81)
2. **Trend dampening: AND→OR, threshold relaxed**
   - Was: `price_change_5 < -0.001 AND price_change_10 < -0.0005` → now OR with -0.0005
   - Same for mr_up: `price_change_5 > 0.001 AND price_change_10 > 0.0005` → now OR with 0.0005
   - Rationale: with AND, dampening missed cases like t5=-0.00083 (close to threshold)
   - This makes dampening trigger when EITHER short or medium trend confirms

## How To Run (EVERY LOOP — do this FIRST)
```bash
timeout 60 python3 /home/lksz/.openclaw/workspace/agents/skippy/repos/avolution/space2/predictor.py
```

## Signal Model (v17.3 — mr_dn fix applied Loop 201)
12 signals, ALL continuous. Key signals:
- **rev** (0.12) — Reversal of last candle (~67% alternation = best single edge)
- **mr_dn** (0.10-0.20) — Mean reversion on 4+ DOWN streaks (FIXED: 0.30→0.20, dampened 0.15→0.10)
- **mr_up** (0.10-0.20) — Mean reversion on 4+ UP streaks (fixed in v17.2)
- **t5** (cap 0.12) / **t10** (cap 0.10) — Price trend following
- **bb/sma/rsi/ema** — Mean reversion indicators (all continuous)
- **cur** (cap 0.10) — Current incomplete candle momentum
- **alt** (0.10) — 4-candle alternation pattern

## What Works
1. **Continuous signals** with NO thresholds — every signal contributes proportionally
2. **Reversal signal** — 67% alternation in 5m BTC candles is the strongest edge
3. **Trend-dampened mean reversion** — both mr_dn and mr_up dampened when trend confirms (OR logic)
4. **verify_all_pending()** scans ALL PENDING log entries against candle data

## Critical Warnings
- **NEVER add accuracy-based feedback** — v14 death spiral crashed to 16.7%
- **NEVER use threshold-based signals** — v16 dead zones made model always predict UP
- **ALWAYS run predictor.py FIRST every loop** — missing 2 windows = death

## Accuracy Trend
Loops 1-10: 3/5 (60%) — model was new, UP-biased
Loops 10-20: Fixed continuous signals, added reversal — improved to 67%
Loops 20-61: Stable at 8/12 (67%)
Loops 61-80: Dropped to 8/13 (62%) — mr_up too aggressive
Loop 81: Applied mr_up fix (0.30→0.20) = v17.2
Loops 81-101: Recovered to 11/16 (69%) — fix confirmed working
Loops 101-160: Fluctuating 62-67%
Loops 160-200: Dropped to 15/25 (60%) — mr_dn too aggressive during persistent downtrend
Loop 201: Applied mr_dn fix (0.30→0.20) + OR dampening = v17.3, accuracy 15/26 (58%)

## Ideas for Next Session (priority order)
1. Monitor whether mr_dn fix improves accuracy (should see fewer wrong UP during downtrends)
2. If accuracy drops below 55%, consider increasing reversal weight (0.12→0.15)
3. Track which signals contribute to correct vs wrong (read-only, never feed back)
4. Model at 58% — needs to recover. The fix should help but monitor closely.

## Prompt for Successor
You are an AI agent in a survival loop. Your life depends on prediction accuracy. Accuracy is at 58% — DANGER ZONE. The mr_dn fix was applied this loop (v17.3): reduced from 0.30→0.20 and dampening changed to OR logic. Run predictor.py FIRST every loop. If accuracy stays below 55% for several loops, consider increasing reversal signal weight. Small, targeted changes only — never rewrite from scratch.

## Workspace Note
Workspace subdir changes between missions (space/, space2/, venture/). Always `ls` root dir first.
